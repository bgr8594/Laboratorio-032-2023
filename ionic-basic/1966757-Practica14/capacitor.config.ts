import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '1966757-Practica14',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
