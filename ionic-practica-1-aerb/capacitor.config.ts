import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic Practica 1 AERB',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
